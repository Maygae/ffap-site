# Dependances installees (reinstallees via npm install)
node_modules/

# Variables d'environnement (contient des identifiants sensibles)
.env

# Fichiers systeme macOS
.DS_Store

# Fichiers uploades par les utilisateurs (photos artistes, images actualites...)
# Versionnes pour que Railway (production) dispose des memes images qu'en local,
# car Railway ne conserve pas de disque persistant entre deploiements sans volume dedie.
# ffap-backend/uploads/

# Dossier de travail temporaire utilise pour importer des photos via les scripts
ffap-backend/photos-a-importer/
